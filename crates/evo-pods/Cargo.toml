[package]
name = "evo-pods"
version = "0.1.0"
edition = "2024"
description = "Evo Pods — works as first-class desktop objects: identity, stage choreography, dim/park containment, gesture declarations (IS-0023)"

[dependencies]
evo-threads = { path = "../evo-threads" }
