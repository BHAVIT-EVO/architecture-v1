[package]
name = "evo-knowledge"
version = "0.1.0"
edition = "2021"

[dependencies]
evo-artifact = { path = "../evo-artifact" }
evo-observation = { path = "../evo-observation" }
uuid = { version = "1", features = ["v4"] }
